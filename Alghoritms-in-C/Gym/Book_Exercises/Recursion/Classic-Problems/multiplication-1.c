#include <stdio.h>
#include <stdlib.h>

#define N 10

typedef struct value_s{
	int num;
	char element[N];
}value_t;

int multiplication_principle(value_t *, char *, int, int, int);

int main(){
	value_t value[3];
	char solution[3];
	int n=3; pos=0; count=0;
	int total;

	value[0].num = 2;
	value[0].element[0] = 'A';

	return 0;
}
